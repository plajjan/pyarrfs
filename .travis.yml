language: python
python:
  - 2.7
before_install:
 - sudo apt-get -y install python-fuse
install: 
 - virtualenv /home/vagrant/virtualenv/python$TRAVIS_PYTHON_VERSION --system-site-packages
 - pip install rarfile
 - python setup.py build
script: cd t; ./test-read.py
